# Repo Memory

Keep repo-scoped lessons here when they are durable and specific to `am-clients`.

Good examples:

- stable template contracts
- route conventions
- interaction rules that should not regress
- deploy or build quirks that recur in this repo

Do not store:

- raw logs
- transcript fragments
- broad Diego workflow notes
- people facts that belong in agent memory
