---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="AM Clients" description="Direct-link client presentations.">
  <main class="home-shell">
    <section class="home-card">
      <p class="home-card__eyebrow">AM Clients</p>
      <h1>Direct-link presentation viewer</h1>
      <p>
        Open your project using the link that was shared with you.
      </p>
    </section>
  </main>
</BaseLayout>

<style>
  .home-shell {
    min-height: 100svh;
    display: grid;
    place-items: center;
    padding: 1.5rem;
  }

  .home-card {
    width: min(100%, 38rem);
    padding: 1.5rem;
    border: 1px solid var(--line-soft);
    border-radius: 1.5rem;
    background: var(--bg-2);
    box-shadow: var(--shadow-soft);
  }

  .home-card__eyebrow {
    margin: 0;
    color: var(--ink-1);
    font-size: 0.78rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0.4rem 0 0.9rem;
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(1.8rem, 4vw, 3rem);
    letter-spacing: -0.05em;
  }

  p {
    margin: 0;
    color: var(--ink-1);
    line-height: 1.6;
  }

</style>
