## Memory

Read `MEMORY.md` at session start. After completing a request that changed code, append a brief entry (date, what changed, what matters next). Writing to MEMORY.md itself does not count as a code change — never chain memory writes.
